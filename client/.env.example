# Base URL of the JBOS backend (Express API).
# Empty/unset → relative '/api' (uses Vite dev proxy locally).
# In production builds, set to your deployed backend origin.
# Example:
#   VITE_API_BASE_URL=https://jbos-api.onrender.com
VITE_API_BASE_URL=
