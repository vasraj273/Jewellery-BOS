{
  "name": "jbos",
  "version": "1.0.0",
  "private": true,
  "description": "Jewellery Business Operating System (JBOS) V1 - Quotation Module",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build:client": "npm run build --prefix client",
    "db:init": "npm run db:init --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
